import React from 'react';
import classes from './Header.module.css';

const Header = () => {
    return (
        <div className={classes.TestCss}>Welcome to 'react' world!</div>
    );
};

export default Header;